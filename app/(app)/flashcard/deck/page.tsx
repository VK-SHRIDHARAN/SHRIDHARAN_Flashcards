import DeckManagement from '@/components/Flashcard/DeckManagement';

export const metadata = {
  title: 'Create Deck - Flashcards',
  description: 'Create and manage your flashcard decks',
};

export default function DeckPage() {
  return <DeckManagement />;
}
