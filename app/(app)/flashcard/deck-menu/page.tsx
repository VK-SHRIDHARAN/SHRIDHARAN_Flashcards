import DeckMenu from '@/components/Flashcard/DeckMenu';

export const metadata = {
  title: 'Deck Menu - Flashcards',
  description: 'Browse and select your flashcard decks',
};

export default function DeckMenuPage() {
  return <DeckMenu />;
}
